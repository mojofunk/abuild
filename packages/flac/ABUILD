#!/bin/bash

function set_pkg_env
{
	PKG_NAME="flac"
	PKG_VERSION="1.3.2"

	PKG_SRC_DIR="${PKG_NAME}-${PKG_VERSION}"
	PKG_SRC_FILE="${PKG_SRC_DIR}.tar.xz"
	PKG_SRC_URL="http://downloads.xiph.org/releases/flac/${PKG_SRC_FILE}"

	PKG_DEPS='libogg'

	PKG_BUILD_TOOL="autotools"
}
